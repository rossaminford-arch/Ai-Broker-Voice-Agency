import { test } from "@playwright/test";

test.skip("marketing journey requires running dev server", async () => {
  // Placeholder so Playwright discovers the suite; real implementation needs running server.
});
